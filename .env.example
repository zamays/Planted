# Planted - Environment Configuration Template
# Copy this file to .env and fill in your own values

# OpenWeatherMap API Key
# Get your free API key at: https://openweathermap.org/api
# Sign up for free at: https://home.openweathermap.org/users/sign_up
OPENWEATHERMAP_API_KEY=your_api_key_here

# Flask Configuration
# CRITICAL: Generate a secure secret key for Flask sessions and CSRF protection
# This key must be kept secret and should be at least 32 characters long
# 
# To generate a secure key, run this command:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
# 
# Copy the output and paste it below:
FLASK_SECRET_KEY=your_secret_key_here

# API Timeout Configuration
# Timeout in seconds for external API requests (weather, location services)
# Default: 10 seconds if not specified
# Recommended range: 5-30 seconds depending on network conditions
API_TIMEOUT=10
